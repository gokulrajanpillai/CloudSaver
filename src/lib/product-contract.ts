export const PRODUCT_CONTRACT = {
  promise: 'Local-first storage intelligence for files you choose to scan.',
  does: [
    'Scans local folders, mounted drives, and supported cloud sources only after you add them.',
    'Builds storage, duplicate, media, and cleanup reports on this device.',
    'Moves selected local files to a reversible review folder with a restore manifest.',
  ],
  doesNot: [
    'Upload file contents, filenames, paths, or scan results by default.',
    'Permanently delete files as the primary cleanup action.',
    'Claim full direct API coverage for every cloud provider until that connector is marked production-ready.',
  ],
}
